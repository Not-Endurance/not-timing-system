@using NTS.Domain.Core.Aggregates
@inherits RankingMenuBehind;

@* //TODO: probably extract this chipset into NotChipGroup component or something similar. Also used in DashboardComponent *@
@if (SelectedRanking != null)
{
    <MudChipSet T="Ranking" SelectedValue="@SelectedRanking" SelectedValueChanged="Select" Class="@Class" Style="@Style">

        @foreach (var grouping in Rankings.GroupBy(x => x.Name))
        {
            <NTextDivider Text="@grouping.Key" />
            @foreach (var ranking in grouping.OrderBy(x => x.Category))
            {
                <MudChip Text="@ranking.Category.ToString()" Value="@ranking" Color="Color.Primary" Style="min-width: 1rem" />
            }
        }
    </MudChipSet>    
}

