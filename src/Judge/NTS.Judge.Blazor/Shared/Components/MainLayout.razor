@using NTS.Judge.Blazor.Shared.Components.Nav
@using NTS.Judge.Blazor.Shared.Components.SidePanels
@using Not.Blazor.Notifier

@inherits LayoutComponentBase

<NMudProviders @ref="_mudProviders"/>

<MudLayout>
    @if (!_hideLayout)
    {
        <MudAppBar Elevation="1">
            <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => ToggleDrawer())" />

            <MudSpacer />

            <MudIconButton Icon="@Icons.Material.Filled.MoreVert" Color="Color.Inherit" Edge="Edge.End" />
        </MudAppBar>

        <MudDrawer @bind-Open="_drawerOpen" Elevation="2">
            <MudDrawerHeader>
                <NHeadline3 Content="NTS Judge v0.2" Style="color:#9ca2b8" />
            </MudDrawerHeader>

            <NavMenu />

            <SidePanel />
        </MudDrawer>
    }

    <MudMainContent>
        <CascadingValue Value="@_mudProviders.ThemeProvider">
            <MudContainer MaxWidth="MaxWidth.Large" Class="py-4">
                @Body
            </MudContainer>

            <BlazorNotifier />
        </CascadingValue>
    </MudMainContent>
</MudLayout>



<style>
    .not-mid-width {
        max-width: 800px;
        margin: auto;
    }
    .not-mid-height {
        min-height: 400px;
    }
</style>
