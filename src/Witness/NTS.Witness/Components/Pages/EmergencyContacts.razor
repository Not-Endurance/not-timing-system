@attribute [Route(EMERGENCY_CONTACTS)]
@inherits EmergencyContactsBehind

<MudStack style="height:100%;" Justify="Justify.Center" Spacing="10" AlignItems="AlignItems.Center">
    <NHeadline Content="@Contacts_string"/>
    @foreach(var contact in Contacts)
    {
        <MudStack Row Spacing="5">
            <MudText Typo="Typo.h5">@contact.Key:</MudText>
            <MudLink Typo="Typo.h5" OnClick="() => OnNumberClick(contact.Value)">@contact.Value</MudLink>
        </MudStack>    
    }
</MudStack>